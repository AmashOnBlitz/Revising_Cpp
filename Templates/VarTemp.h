/*=============================================================
Project    : Revising CPP
file       : ${filename}
Created By : ${name}
On         : ${date}
=============================================================*/

#ifndef REVISE_CPP_${upper:${stem}}_HEADER
#define REVISE_CPP_${upper:${stem}}_HEADER

int main_${pascal:${stem}}(int argc, char const *argv[]) {
    return 0;
}

#endif //!REVISE_CPP_${upper:${stem}}_HEADER